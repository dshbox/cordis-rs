# Changelog

All notable changes to this project are documented in this file.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and
[Semantic Versioning](https://semver.org/spec/v2.0.0.html). From 0.1.2 onward,
entries are generated by [release-plz](https://release-plz.dev/) from
[Conventional Commits](https://www.conventionalcommits.org/).

## [0.4.8](https://github.com/dshbox/cordis-rs/compare/v0.4.7...v0.4.8) - 2026-08-18

### Fixed

- prune dead plugin runtimes from registry read APIs
- dispose adopted children when an effect is cancelled

### Other

- mark await_ready/dispose_async as synchronous pass-throughs
- skip log record assembly when nothing would consume it

## [0.4.7](https://github.com/dshbox/cordis-rs/compare/v0.4.6...v0.4.7) - 2026-08-18

### Fixed

- bound fiber transition waits so hung plugins fail fast

### Other

- Merge pull request #27 from dshbox/fix/issue-25-transition-wait

## [0.4.6](https://github.com/dshbox/cordis-rs/compare/v0.4.5...v0.4.6) - 2026-08-17

### Fixed

- harden the core lifecycle against races and mis-typed configs

### Other

- Merge pull request #22 from dshbox/fix/audit-major

## [0.4.5](https://github.com/dshbox/cordis-rs/compare/v0.4.4...v0.4.5) - 2026-08-17

### Added

- resolve plugins from dynamic libraries behind the dynamic feature

### Other

- reflect dynamic plugins and HMR in the root READMEs

## [0.4.4](https://github.com/dshbox/cordis-rs/compare/v0.4.3...v0.4.4) - 2026-08-17

### Other

- align READMEs with the current workspace and feature set

## [0.4.3](https://github.com/dshbox/cordis-rs/compare/v0.4.2...v0.4.3) - 2026-08-17

### Other

- add crate READMEs and an ecosystem overview

## [0.4.2](https://github.com/dshbox/cordis-rs/compare/v0.4.1...v0.4.2) - 2026-08-17

### Added

- add Fiber::ptr_eq and expose PluginHandle::plugin
- add non-blocking Fiber::idle probe

### Other

- record review resolutions and align effect test locks

## [0.4.1](https://github.com/dshbox/cordis-rs/compare/v0.4.0...v0.4.1) - 2026-08-17

### Other

- restructure into a virtual cargo workspace

## [0.4.0](https://github.com/dshbox/cordis-rs/compare/v0.3.0...v0.4.0) - 2026-08-17

### Fixed

- keep acronym runs intact when hyphenating logger names
- roll back property declaration on failed provide and prune dead fibers

### Other

- fix stale wait references left by the try_wait rename
- record pending review findings in TODO.md
- [**breaking**] rename Fiber::wait to Fiber::try_wait
- drop dead buffer-clear branch in logger send
- document set-value notification gap and on_async blocking caveat

## [0.3.0](https://github.com/dshbox/cordis-rs/compare/v0.2.2...v0.3.0) - 2026-08-16

### Fixed

- accept config updates on inactive fibers
- close lost-wakeup race in fiber refresh

### Other

- bump install example to 0.3
- document intercept-event gaps, isolation slots, and panic semantics
- [**breaking**] remove dead public API left over from the port
- [**breaking**] return Option<Context> from Fiber::context

## [0.2.2](https://github.com/dshbox/cordis-rs/compare/v0.2.1...v0.2.2) - 2026-08-16

### Other

- apply rustfmt to effect tests
- cover fiber state-machine corners and parallel error aggregation
- cover recursive disposal of adopted effects

## [0.2.1](https://github.com/dshbox/cordis-rs/compare/v0.2.0...v0.2.1) - 2026-08-16

### Fixed

- honest lifecycle readiness and root-fiber restart guard

### Other

- warn against same-thread-blocking futures in transitions

## [0.2.0](https://github.com/dshbox/cordis-rs/compare/v0.1.2...v0.2.0) - 2026-08-16

### Added

- [**breaking**] add source chaining to CordisError

### Other

- update custom plugin example for borrowed Plugin::inject
- [**breaking**] rename get_effects and borrow Plugin::inject

## [0.1.2](https://github.com/dshbox/cordis-rs/compare/v0.1.1...v0.1.2) - 2026-08-16

### Other

- manage releases with release-plz and seed CHANGELOG.md

## [0.1.1] - 2026-08-16

### Fixed

- `block_on`: hold the Parker mutex while waking, closing a lost-wakeup window
  that could hang single-shot cross-thread wakes permanently
- fiber: fail fast on reentrant `restart()`/`dispose()` from lifecycle
  callbacks (status listeners, disposers) instead of deadlocking on the
  transition mutex
- events: evaluate user `ContextFilter`s outside the events state lock instead
  of deadlocking filters that re-enter the events API
- events: remove `once` listeners on invocation (upstream cordis parity);
  listeners skipped by an earlier bail or error stay registered
- registry: remove rejected plugin fibers from the registry so
  `len()`/`contains()` no longer report phantom records
- fiber: tolerate fibers that outlive their root context instead of panicking
- fiber: return an error from `update()` on the root fiber instead of
  panicking
- effect: close the `adopt()` check-then-act race that could leak a child's
  cleanup when the parent is disposed mid-adoption

### Performance

- fiber/reflect: dependency epochs are generation vectors aligned with inject
  order (no per-refresh string clones); `implementation_epoch` clones the
  service value only when an availability check exists; reconcile decides
  under one lock without cloning snapshots
- fiber: `update()` reuses its pre-validation result instead of validating the
  same config twice
- reflect: single-lock service lookups in `value()`/`set_value()`; availability
  predicates borrow via `downcast_ref`
- registry/reflect: service notifications use a per-service fiber index
  instead of scanning and upgrading every fiber in the registry
- logger: buffer stores `Arc<Message>` (cloning moves to read time); exporters
  iterate a registration snapshot; `Exporter::config_ref()` avoids deep-cloning
  level/formatter maps per message
- events: skip `internal/dispatch` meta-event construction when nobody listens

### Other

- `resolve_service_config` no longer requires `T: Clone` (semver-compatible
  relaxation)
- `Exporter` gained a default `config_ref()` method (non-breaking addition)

## [0.1.0] - 2026-08-16

### Added

- Initial release: a faithful, runtime-agnostic Rust port of the Cordis 4.x
  plugin framework core (the plugin framework at the core of DeepSeek
  Harness) — scoped dependency injection, lifecycle-owned effects, five-mode
  event dispatch, configuration interception, and structured logging, with
  zero external dependencies

[0.1.1]: https://github.com/dshbox/cordis-rs/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/dshbox/cordis-rs/releases/tag/v0.1.0
